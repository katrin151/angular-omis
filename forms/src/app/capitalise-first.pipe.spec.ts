import { CapitaliseFirstPipe } from './capitalise-first.pipe';

describe('CapitaliseFirstPipe', () => {
  it('create an instance', () => {
    const pipe = new CapitaliseFirstPipe();
    expect(pipe).toBeTruthy();
  });
});
